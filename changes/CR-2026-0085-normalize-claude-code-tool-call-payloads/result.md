# Result

- Canonicalized Claude Code Bash, file, search, and dynamic tool payloads at the adapter boundary.
- Preserved canonical fields by tool-call ID through successful and failed result events, with the existing unmatched-result fallback.
- Added fake CLI, adapter contract, and persisted workspace segment coverage.
