# Result

- Routed Claude Code chat turns through the CLI backend while preserving HTTP routing for Anthropic selectors.
- Added conversation session resume persistence and one-shot fresh-session recovery after a failed CLI resume.
- Added Claude Code validation, static model aliases, frontend provider/model choices, and graph-editor compatibility through shared options.
- Extended the fake CLI and contract coverage for resume recovery.
