[
  {
    "id": "coding-agent-cargo-cutover",
    "summary": "Coding-agent execution is owned by Rust crates and validated through Cargo tests.",
    "implementation_contract": "Normal Spark agent execution must use crates/spark-agent-adapter and crates/unified-llm-adapter, not Python runtime packages.",
    "validation_expectations": "Run cargo fmt --all -- --check, cargo test --workspace --all-features, npm --prefix frontend run test:unit, and npm --prefix frontend run build."
  }
]
